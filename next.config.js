/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
    images: {
      domains: ['dev-kdurkin-sql.pantheonsite.io'], // Add your WordPress domain here
    },
  };
